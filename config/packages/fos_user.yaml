fos_user:
  db_driver: orm # other valid values are 'mongodb' and 'couchdb'
  firewall_name: main
  user_class: AppBundle\Entity\FOS_User
  from_email:
    address: "truc@domain.com"
    sender_name: "truc@domain.com"